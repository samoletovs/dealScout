# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# dealScout — never commit secrets or personal config
.env
config.local.yaml
out/

# Monitor ledger — runtime state, not source. Losing it costs one noisy run.
state/

# Local-only personal-info pattern overrides
scripts/.leak-patterns.local.txt
